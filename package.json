{
  "name": "base",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "full-install": "cd client && npm install && cd ../server && npm install",
    "install": "cd client && npm install && cd ../server && npm install",
    "start": "node server/index.js"
  },
  "author": "",
  "license": "ISC"
}
